<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.doan_appdaynauan">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_logo"
        android:label="Bếp Nhà Làm"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DOAN_AppDayNauAn">
        <activity android:name=".AppInFoActivity"></activity>
        <activity android:name=".FavoriteActivity" />
        <activity android:name=".SearchActivity" />
        <activity android:name=".AllFoodActivity" />
        <activity android:name=".InformationFood" />
        <activity
            android:name=".MenuActivity"
            android:theme="@style/Theme.DOAN_AppDayNauAn1">

        </activity>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>